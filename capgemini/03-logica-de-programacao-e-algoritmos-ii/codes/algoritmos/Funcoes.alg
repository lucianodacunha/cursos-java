algoritmo "Funções"

var
    resultado: inteiro

    funcao somar(a, b: inteiro): inteiro
        retorne (a + b)
    fimfuncao
inicio

    escreval("Valor da soma de 3 + 3:", somar(3, 3))

fimalgoritmo
